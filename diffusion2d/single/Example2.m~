l = 2;
dx = 0.1;
dt = 0.001;
T = 4;
X = meshgrid(-l+dx/2:dx:l-dx/2);
Y = X.';
N = length(X);

rho0 = zeros(N,N);
rho0(X<3 & X>-3 & Y<3 & Y>-3) = 1;
syms W(x,y)
W(x,y) = x^2/2 - log(x^2 + y^2)/2 + y^2/2;

rhoR = single2d (rho0,l, W, dt, T, 'v', H);